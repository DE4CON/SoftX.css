/**
 * @name SoftX (Clean Sidebar – No Icon Effects)
 * @author Gibbu (edit by NCK)
 * @version 2.0.6
 * @description Removes all sidebar icon effects, restores default spacing and alignment.
*/

@import url('https://discordstyles.github.io/SoftX/SoftX.css');
@import url("https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500&display=swap");

:root {
  --background-image: url("https://i.imgur.com/Nglfni6.png");
  --background-blur: 0px;
  --accent: hsl(164 100% 45%);
  --accent-text: hsl(0 0 0%);
  --glow-intensity: 0;
  --members-width: 240px;
  --guilds-width: 85px;
  --server-icon-size: 40px;
  --chat-avatar-size: 34px;
  --opacity: 0.85;
  --font: "Inter";

  --rs-small-spacing: 2px;
  --rs-medium-spacing: 4px;
  --rs-large-spacing: 4px;
  --rs-small-width: 1.5px;
  --rs-medium-width: 2px;
  --rs-large-width: 2px;
  --rs-online-color: #43b581;
  --rs-idle-color: #faa61a;
  --rs-dnd-color: #f04747;
  --rs-offline-color: #636b75;
  --rs-streaming-color: #643da7;
  --rs-invisible-color: #747f8d;
  --rs-phone-color: var(--rs-online-color);
  --rs-phone-visible: block;
}

/* === Scale Normalization === */
html, body, .app-2CXKsg {
  zoom: 0.95 !important;
}

/* === Sidebar cleanup (no glow, no transform, no extra spacing) === */
.guilds-2JjMmN {
  background: rgba(0,0,0,0.45) !important;
  backdrop-filter: none !important;
  box-shadow: none !important;
}

.wrapper-1Rf91z,
.scroller-3X7KbA,
.listItem-3SmSlK,
.folder-1hbNCn,
.wrapper-38slSD {
  animation: none !important;
  transform: none !important;
  transition: none !important;
  box-shadow: none !important;
  filter: none !important;
}

/* === Icons clean (no rounding, no mask, no glow, fully aligned) === */
.listItem-3SmSlK,
.childWrapper-1j_1ub,
.icon-3o6xvg,
.folder-1hbNCn,
.folderIconWrapper-226oVY,
.expandedFolderBackground-3ZDF05 {
  border-radius: 0 !important;
  mask: none !important;
  -webkit-mask: none !important;
  overflow: visible !important;
  clip-path: none !important;
  box-shadow: none !important;
  transform: none !important;
  filter: none !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  margin: 4px auto !important;
}

/* === Perfect centering and sizing for icons/folders === */
.listItem-3SmSlK {
  display: flex !important;
  justify-content: center !important;
  align-items: center !important;
  width: var(--server-icon-size) !important;
  height: var(--server-icon-size) !important;
  padding: 0 !important;
  margin: 5px 0 !important;
}

.guilds-2JjMmN .scroller-3X7KbA {
  display: flex !important;
  flex-direction: column !important;
  align-items: center !important;
  gap: 8px !important;
}

/* === No hover transforms/glow === */
.listItem-3SmSlK:hover,
.wrapper-3kah-n:hover {
  transform: none !important;
  box-shadow: none !important;
  filter: none !important;
}

/* === Text and readability === */
.username-h_Y3Us {
  font-size: 13.5px !important;
  font-weight: 400 !important;
}

.markup-eYLPri {
  font-size: 14px !important;
  line-height: 1.35 !important;
}

/* === App background === */
.appMount-2yBXZl {
  background-color: rgba(0, 0, 0, 0.6) !important;
  backdrop-filter: blur(var(--background-blur));
}
